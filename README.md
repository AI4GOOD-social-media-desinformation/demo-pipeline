# demo-pipeline
The goal of this repo is to quickly create and evaluate modules in different pipeline setups

## Setup

```bash
pip install -e .
```
This will install the required dependencies and the package in editable mode.

## Folder Structure
- `data/`: Contains the datasets, data processing scripts, and data loaders.
-  `experiments/`: Contains experimental notebooks and scripts to test and validate quickly hypothesis